## 1. C1 Proposal And Release Contract

- [x] 1.1 Verify fresh takeover evidence, exact clean `HEAD == origin/main`, absent local/remote v1.0.0 tag and GitHub Release, Owner-authorized public promise, no-next-target state, GitHub-Release-only publication, and no-deployment boundary.
- [x] 1.2 Define the exact tracked-path allowlist, annotated-tag Los Angeles date rule, byte-deterministic changelog payload, immutable remote-tag and retry rules, exact tag target, and C1-C4 plus post-archive gate order.
- [x] 1.3 Start one fresh runtime-proven C1 PM lifecycle and one fresh Claude Advisor lifecycle, record Stage Session and Runtime Session IDs, and obtain independent PM, Advisor, and Reviewer decisions on one exact fingerprint with no unresolved P0/P1.
- [x] 1.4 Close C1 roles sequentially and record transition evidence; no C1 GO, validation freshness, or authorization state carries into C2.

## 2. C2 Public Documentation Conversion

- [x] 2.1 Start fresh runtime-proven C2 PM and Claude Advisor lifecycles with new Stage and Runtime Session IDs, plus a fresh Worker and Reviewer scope; do not inherit C1 decisions.
- [x] 2.2 Update every README version/status passage, including first-screen status, Current Status, progressive-profile prose, and historical release examples, to public v1.0.0, historical v0.4.18, no next development target, and explicit limitations.
- [x] 2.3 Convert the v1.0.0 changelog entry to a published release section using the current Los Angeles annotated-tag date, replace `## [Unreleased]` with exactly `## Releases`, and remove self-negating development/publication text while retaining limitations.
- [x] 2.4 Align ROADMAP, TODO, VALIDATION, TRACEABILITY, INSTALLATION, ADAPTERS, and the three profile labels to exactly `Version: v1.0.0 release template.` while preserving the historical v0.4.18 review-packet template.
- [x] 2.5 Preserve English-only public documentation and avoid new runtime-support, deployment, benchmark, token-saving, storage-saving, or quality-guarantee claims.

## 3. C2 Deterministic Validation

- [x] 3.1 Rotate public/development constants and contradiction checks so public v1.0.0 with no next target passes and stale v1.0.0-development or current-public v0.4.18 text fails in both declaration and version-before-phrase prose forms.
- [x] 3.2 Keep provisional structural thresholds, warning-only size bands, interim `hierarchical-required`, and unimplemented Hierarchical storage checks active without a development target.
- [x] 3.3 Replace the three development-template marker checks with the exact v1.0.0 release-template literal and retain the historical v0.4.18 review-packet literal.
- [x] 3.4 Add positive and negative fixtures for tag, Release, publication, annotated-tag date, historical version, no-next-target, no-deployment semantics, and the actual former README sentence form `The v1.0.0 development target ...`.
- [x] 3.5 Run shell syntax, strict OpenSpec, repository-local validation, English-only checks, diff checks, and sensitive-content/path scans on the exact implementation target.
- [x] 3.6 Obtain fresh independent C2 PM, Advisor, and Reviewer approval with no unresolved P0/P1 and verify that no path outside the exact allowlist changed.
- [x] 3.7 Close Worker, Reviewer, PM, and Advisor sequentially and record the C2-to-C3 transition; no C2 GO or validation freshness carries into C3.

## 4. C3 Release-Status Git Closeout

- [x] 4.1 Start fresh runtime-proven C3 PM and Claude Advisor lifecycles plus fresh Reviewer participation; recheck the current `America/Los_Angeles` date before freezing release-facing documentation.
- [ ] 4.2 Complete staged scope, secret scan, PM/Advisor/Reviewer precommit, an English release-status commit message with `Core Changes`, `Rationale`, `Remaining Work`, and `Review Notes`, actual-commit review, push, remote equality, no-GitHub-Actions metadata, and post-push reviews.
- [ ] 4.3 Confirm README and every release-facing surface agree on public v1.0.0, historical v0.4.18, no next target, provisional thresholds, warning-only bands, interim `hierarchical-required`, unimplemented Hierarchical storage, and no deployment.
- [ ] 4.4 Close Reviewer, PM, and Advisor sequentially and record the C3-to-C4 transition; no C3 GO or validation freshness carries into C4.

## 5. C4 Pre-Archive Readiness

- [ ] 5.1 Start fresh runtime-proven C4 PM and Claude Advisor lifecycles plus fresh Reviewer participation without inheriting C3 state.
- [ ] 5.2 Prepare and review the accepted-spec sync with Purpose exactly `Define the authoritative current public release state, validation, lifecycle gates, exact release target, GitHub-Release-only publication boundary, and closeout requirements for v1.0.0 while preserving v0.4.18 as historical evidence.`, both renamed public-state requirements, validation of the synchronized spec, and a plan to archive with `--skip-specs` so no delta is applied twice; assert that no accepted Purpose or requirement still calls v0.4.18 current.
- [ ] 5.3 Run strict active-state OpenSpec and repository-local validation, scope and sensitive-content checks, and obtain fresh PM/Advisor/Reviewer confirmation that the synced change is ready to archive.
- [ ] 5.4 Record that post-archive evidence will remain in this Codex task's role/command transcript, Git commit and tag objects, remote refs, and GitHub API/Release metadata, never in later edits to the archived checklist.
- [ ] 5.5 Review the complete external archive and publication runbook below, including truthful `.github`/zero-context evidence, exact peeled-target proof independent of `targetCommitish`, immutable remote-tag handling, incomplete-publication retry behavior, three separate fresh non-OpenSpec role groups for tag, Release, and closeout, and no deployment.
- [ ] 5.6 Confirm every preceding checkbox is complete and obtain C4 Reviewer, PM, and Advisor authorization to cross the archive boundary while keeping the exact C4 PM and Advisor runtime sessions open for fresh archive, actual-commit, push, and post-push checkpoints.

## Post-Archive Operational Runbook

This runbook is durable external operational procedure, not an OpenSpec
checklist. After item 5.6 is checked, do not edit `tasks.md` in the archive.

1. After the requirements and exact v1 Purpose have already been synchronized and validated, archive with `openspec archive publish-v1-0-0-release-status --yes --skip-specs`, verify zero active changes, run strict archived-state and no-active local validation, and confirm the accepted Purpose/requirements contain no current-public v0.4.18 contradiction.
2. Continue the exact runtime-proven C4 PM and Claude Advisor sessions, with fresh Review IDs, Attempt IDs, fingerprints, decisions, validation evidence, and Reviewer participation; secret-scan, commit with the required four-section message, review the actual commit, push normally, verify `HEAD == origin/main`, clean state, and truthfully report absent `.github`, statuses, and check runs as “no GitHub Actions configured.”
3. Complete fresh C4 post-push PM, Advisor, and Reviewer review, freeze that exact archive commit as the only v1.0.0 target, do not advance HEAD afterward, and close C4 Reviewer, PM, and Advisor sequentially.
4. From the frozen commit, extract the v1.0.0 changelog section with LF line endings, exclude the next heading, remove all trailing blank lines, append exactly one final LF byte, materialize one uniquely named `/tmp` payload, and record its SHA-256.
5. Start fresh short-lived non-OpenSpec tag-gate PM, Claude Advisor, and Reviewer sessions. Recheck Los Angeles date and all evidence immediately before local tag creation. If the date changed, stop and rebuild/re-review before any tag exists. Once created, freeze the tag-bound date and never move or delete either the local or remote tag.
6. Create annotated tag `v1.0.0` at exactly the frozen commit with message `v1.0.0 - Progressive Leader State Profiles`, push only that tag normally, verify local/remote tag objects and peeled commit targets, then close the tag Reviewer, PM, and Advisor sequentially.
7. Start new fresh short-lived non-OpenSpec payload/Release PM, Claude Advisor, and Reviewer sessions against the verified tag and exact payload; record the action-time date without changing the frozen tag-bound documented date.
8. Create one formal GitHub Release titled exactly `v1.0.0` using the exact payload with `draft=false` and `prerelease=false`. If creation fails, publication remains incomplete and may only retry on the unchanged tag, peeled commit, title, and payload; never move/delete the remote tag.
9. Verify through GitHub metadata/API the Release URL, `publishedAt`, tag, annotated-tag object and peeled commit independently of `targetCommitish`, draft/prerelease state, UTF-8 body byte equality and SHA-256, synchronized clean repository, README/public-doc alignment, and that deployment did not occur; then close the Release Reviewer, PM, and Advisor sequentially.
10. Start a third fresh short-lived non-OpenSpec PM, Claude Advisor, and Reviewer group for post-publication closeout, obtain independent decisions on the verified final state, then close Reviewer, PM, and Advisor sequentially.

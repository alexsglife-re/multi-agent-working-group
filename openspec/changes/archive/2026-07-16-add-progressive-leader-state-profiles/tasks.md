## 1. Profile Semantics

- [x] 1.1 Update `SKILL.md` and mandatory references with Compact, Standard, and `hierarchical-required` selection rules, deterministic counting units, and current-active-only scope; if a new reference file is created, register it in required-reference validation and add mandatory `SKILL.md` routing, otherwise record which existing reference owns the rules.
- [x] 1.2 Define the complete accepted retention-floor routing, profile-selection basis, canonical ownership, and derived-copy freshness rules without duplicating full durable reasoning into active state.
- [x] 1.3 Define event-driven freshness checkpoints, promotion behavior, lower demotion exit bands, two-checkpoint hysteresis, and the no-background-mutation boundary.
- [x] 1.4 Document the three-way pilot rule consistently: provisional structural thresholds select profiles but never block work by themselves; line/byte bands only warn; semantic/control failures remain fail-closed.

## 2. Profile-Aware Instances

- [x] 2.1 Record the static-template decision: add separate reduced Compact and Standard templates, not a renderer, and use `examples/leader-state-profiles/` as the rendered-fixture home.
- [x] 2.2 Create and measure one repo-grounded Compact feasibility fixture with the complete applicable retention floor; stop and amend the provisional line baseline if it exceeds the 140-line warning.
- [x] 2.3 Implement a genuinely reduced Compact active-state and handoff template with lightweight selection attestation and conditional accepted fields.
- [x] 2.4 Implement a single-file Standard active-state and handoff template with stable sections and source/freshness metadata only for actual derived copies.
- [x] 2.5 Preserve the current-state projection, claim-indexed evidence index, archive-note capability, and stable external evidence pointers in both profiles.
- [x] 2.6 Add an Opportunity successor skeleton and update complete successor rendering for Recommended/Required or actual handoff without authority, approval, freshness, or continuity inheritance.

## 3. Documentation and Adoption

- [x] 3.1 Update `README.md` with the progressive-profile rationale, small/medium/large applicability, warning-only pilot status, and the explicit Hierarchical follow-on limitation.
- [x] 3.2 Update validation and installation/adoption documentation with migration guidance from copied full templates and with the non-semantic validator boundary.
- [x] 3.3 Record the Owner-reported prior 4,000-line outcome accurately and use only labeled synthetic stress evidence unless the original inventory becomes available.
- [x] 3.4 Declare `v1.0.0 — Progressive Leader State Profiles` as the development target across README, CHANGELOG, ROADMAP, TODO, VALIDATION, and version-marker validation while preserving `v0.4.18` as current public until a separately gated release-status closeout.

## 4. Fixtures and Local Validation

- [x] 4.1 Add representative repo-grounded small and medium rendered fixtures with cited provenance, expected profile, deterministic counts, complete triggered retention floor, omitted inapplicable sections, physical-line/UTF-8-byte measurements, and non-blocking warning assertions.
- [x] 4.2 Add takeover-direct-to-Standard, above-Standard, dependency/conflict-edge and overlap-edge boundary, transition, and two-checkpoint hysteresis fixtures with complete classification counts.
- [x] 4.3 Add missing-field, stale-pointer, conflicting-source, invalid-selection-basis, and size-warning-only negative fixtures.
- [x] 4.4 Add a clearly labeled synthetic large-state stress fixture when no original 4,000-line inventory is available.
- [x] 4.5 Extend `scripts/validate-local.sh` with deterministic profile markers, physical-line and UTF-8-byte measurements, structural fixture checks, warning-only size results, registration of every new profile reference/template, mandatory `SKILL.md` routing, and a ratcheted verified hard-boundary anchor floor.
- [x] 4.6 Update `docs/VALIDATION.md` to state exactly what profile checks prove and what semantic, authorization, role-runtime, and factual claims they cannot prove.

## 5. Verification and Follow-ons

- [x] 5.1 Run representative small, medium, takeover, above-Standard, successor, and negative fixture checks and record results without calling warning-only size findings failures.
- [x] 5.2 Run strict OpenSpec and repository-local validation, record any installed-global drift truthfully, and obtain fresh PM and Advisor implementation reviews with no unresolved P0/P1.
- [x] 5.3 Record the named `add-hierarchical-leader-state-sharding` follow-on, its entry condition (real large-project or pilot evidence showing Standard is insufficient), and required root/card/shard evidence in `docs/ROADMAP.md`; do not create that OpenSpec change in this version.
- [x] 5.4 Record the named `calibrate-leader-state-profile-thresholds` follow-on, its entry condition (sufficient profile measurements plus an Owner decision to consider enforceable thresholds), and required calibration evidence in `docs/ROADMAP.md`; do not create that OpenSpec change in this version.
